var count = require('../../count');

exports.isCount = {
  setUp: function (done) {
    done();
  },

  'is an array of words': function (test) {
    // TODO: test Get the sequence of words that spells out the count count(*).words()
    test.done();
  }
};
